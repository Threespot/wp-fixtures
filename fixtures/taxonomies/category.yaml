# Stub fixture. Replace with the canonical starter category set.
# Each entry: name (required), slug (optional, derived from name),
#             description (optional), parent (optional, references another slug).

- name: News
  slug: news
- name: Events
  slug: events
- name: Press Releases
  slug: press-releases
  description: Official press communications
  parent: news
